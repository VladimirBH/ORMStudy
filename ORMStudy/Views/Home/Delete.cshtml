@model ORMStudy.Models.Students
@{
    ViewData["Title"] = "Удалить студента";
}

<h1>Удалить студента</h1>

<h3>Вы уверены, что хотите удалить этого студента?</h3>
<div>
    <h4>Студент</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Фамилия</dt>
        <dd class="col-sm-10">@Model.LastName</dd>
        <dt class="col-sm-2">Имя</dt>
        <dd class="col-sm-10">@Model.Name</dd>
        <dt class="col-sm-2">Пол</dt>
        <dd class="col-sm-10">@Model.Gender</dd>
        <dt class="col-sm-2">Команда</dt>
        <dd class="col-sm-10">@Model.Team.TeamName</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" /> |
        <a asp-action="Index">Назад к списку</a>
    </form>
</div>